Multi-select and deferred settings (anything behind a Save/Apply).

```jsx
<Checkbox checked={a} onChange={t} label="Stream tool output" description="Shows stdout as it arrives" />
<Checkbox indeterminate label="Selected files" />
```
