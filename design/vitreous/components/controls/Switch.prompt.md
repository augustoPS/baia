Applies instantly. Label sits trailing in settings rows; in a grouped row the label lives in the row and `label` is omitted.

```jsx
<Switch checked={auto} onChange={() => setAuto(!auto)} label="Auto-approve safe tools" />
```
