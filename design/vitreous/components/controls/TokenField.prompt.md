Compound queries and lists of things. Return commits a token, Backspace on an empty draft removes the last one.

```jsx
<TokenField label="Scope" tokens={["repo:atlas", "tool:bash"]} onAdd={add} onRemove={remove} />
```
