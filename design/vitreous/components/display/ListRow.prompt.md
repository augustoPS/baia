The workhorse row for sidebars, file lists, message lists and outlines.

```jsx
<ListRow selected title="Refactor auth guard" subtitle="12 tools · 4m ago" trailing={<Badge tone="positive">done</Badge>} />
<ListRow disclosure={open} depth={1} title="src/agents" onClick={toggle} />
```
