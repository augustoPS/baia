Right-hand pane for attributes of the current selection — never primary navigation.

```jsx
<Inspector tabs={["Run", "Context", "Cost"]} tab={t} onTab={setT}
  sections={[{ title: "Model", content: <PopUpButton … /> }, { title: "Tools", content: <>…</> }]} />
```
