Group related content inside a pane. Don't nest a `Box` in a `Box` — use `Divider` inside one instead.

```jsx
<Box title="Context" subtitle="4 files attached" actions={<Button size="small">Add…</Button>}>…</Box>
```
