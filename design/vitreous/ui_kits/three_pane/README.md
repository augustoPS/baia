# UI kit — Three-pane content app

The classic sidebar + list + reading-pane layout (mail, notes, feeds), built on `SplitView`.

- Sidebar filter, thread selection, and a compact/regular density switch are live.
- The reading pane shows the prose treatment: 72ch column, `text-wrap: pretty`, secondary-alpha body,
  and an attached-run `Material` card.
