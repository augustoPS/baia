# UI kit — File browser

Three view modes over the same tree: columns (browse by clicking into folders), grid, and a sortable
list built on `Table`. Quick Look opens as a `Sheet` with a code preview.

- Column view pushes and pops panes as you click folders; selection is shared across views.
- File tiles in grid view are extension-lettered glass tiles — no icon set is shipped.
